Just hacking around using Socket.IO

To get it running:
    * start the server, it listens on port 8000
    * open your browser to http://localhost:8000
    * have fun

Dependencies:
    * Node, obviously.
    * Connect
    * Socket.IO

